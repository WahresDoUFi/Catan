using TMPro;
using UnityEngine;
using UnityEngine.UI;

namespace UI.Trade
{
    public class PlayerTradeCard : MonoBehaviour
    {
        [SerializeField] private Image receiveIcon;
        [SerializeField] private TextMeshProUGUI receiveText;
        [SerializeField] private Image payIcon;
        [SerializeField] private TextMeshProUGUI payText;
    }
}
